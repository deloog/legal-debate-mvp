{
  "$schema": "https://json.schemastore.org/tsconfig",
  "extends": "./tsconfig.json",
  "compilerOptions": {
    /**
     * TypeScript Strict Mode - Pragmatic Configuration
     *
     * This configuration provides 90% of the benefits of strict mode with
     * significantly less migration effort.
     *
     * KEY DIFFERENCES FROM FULL STRICT:
     * - strict: false (individual options enabled instead)
     * - noUncheckedIndexedAccess: false (prevents 1598 undefined/null errors)
     *
     * BENEFITS:
     * - Catches most type safety issues
     * - Enables noImplicitAny, strictNullChecks, strictFunctionTypes
     * - Allows reasonable tradeoffs for array/object access patterns
     *
     * USAGE:
     * 1. For development/testing: Use this config to check code
     *    npx tsc --noEmit --project tsconfig.strict-pragmatic.json
     *
     * 2. To enable in production: Copy these compilerOptions to main config.
     */

    /**
     * Enable strict type checking options individually
     * (strict: false allows us to be selective)
     */
    "strict": false,

    /**
     * Enable key strict options
     */

    /**
     * Strict null checking
     * Null and undefined must be explicitly handled
     *
     * Example:
     * ❌ const name: string = getName(); // May be undefined
     * ✅ const name: string | undefined = getName();
     * ✅ if (name) { console.log(name); }
     */
    "strictNullChecks": true,

    /**
     * No implicit 'any' types
     * Variables, parameters, and return types must have explicit types
     *
     * Example:
     * ❌ function add(a, b) { return a + b; }
     * ✅ function add(a: number, b: number): number { return a + b; }
     */
    "noImplicitAny": true,

    /**
     * Strict function types
     * Function parameter bivariance is disabled
     * Prevents unsafe function type assignments
     */
    "strictFunctionTypes": true,

    /**
     * Strict property initialization
     * Class properties must be initialized in constructor
     *
     * Example:
     * ❌ class User { name: string; }
     * ✅ class User { name: string; constructor(name: string) { this.name = name; } }
     * ✅ class User { name!: string; } // Definite assignment assertion
     */
    "strictPropertyInitialization": true,

    /**
     * Strict bind call apply
     * Checks if call(), bind(), and apply() are used correctly
     */
    "strictBindCallApply": true,

    /**
     * No implicit 'this'
     * 'this' must be explicitly typed when used in functions
     *
     * Example:
     * ❌ function getName() { return this.name; }
     * ✅ function getName(this: { name: string }) { return this.name; }
     */
    "noImplicitThis": true,

    /**
     * Additional type safety options (recommended)
     */

    /**
     * No unused locals
     * Report errors on unused local variables
     */
    "noUnusedLocals": true,

    /**
     * No unused parameters
     * Report errors on unused parameters
     * Note: Prefix with _ to intentionally ignore: function test(_unused: string)
     */
    "noUnusedParameters": true,

    /**
     * No implicit returns
     * Report error when not all code paths return a value
     */
    "noImplicitReturns": true,

    /**
     * No fallthrough cases in switch
     * Report errors for fallthrough cases in switch statements
     */
    "noFallthroughCasesInSwitch": true,

    /**
     * No unchecked indexed access
     * Report errors when accessing array/object indices that might be undefined
     *
     * NOTE: Disabled in pragmatic mode to avoid 1598+ errors
     *
     * Example (when enabled):
     * ❌ const arr: string[] = ['a', 'b'];
     * ❌ const first = arr[0]; // Type: string | undefined
     * ✅ const first = arr[0]!; // Non-null assertion
     * ✅ const first = arr[0] ?? ''; // Nullish coalescing
     */
    "noUncheckedIndexedAccess": false,

    /**
     * No implicit override
     * Require explicit @override annotation for overridden methods
     */
    "noImplicitOverride": true,

    /**
     * No property access from index signature
     * Disallow access to properties of index signatures using . notation
     */
    "noPropertyAccessFromIndexSignature": false,

    /**
     * Allow unused labels
     * Allow unused labels (continue labels, etc.)
     */
    "allowUnusedLabels": false,

    /**
     * Allow unreachable code
     * Report errors for unreachable code
     */
    "allowUnreachableCode": false,

    /**
     * Exact optional property types
     * Make optional properties exact (no matching undefined properties)
     */
    "exactOptionalPropertyTypes": false,

    /**
     * Skip lib check
     * Skip type checking of declaration files
     * Useful when third-party type definitions have issues
     */
    "skipLibCheck": true
  },

  /**
   * Exclude patterns
   * These patterns are excluded from type checking
   */
  "exclude": [
    "node_modules",
    ".next",
    "out",
    "build",
    "coverage",
    "test-results",
    "playwright-report",
    "*.config.js",
    "*.config.mjs",
    "dist",
    "data",
    "backups",
    "public/uploads",
    "scripts/archive",
    "scripts/crawler/archive"
  ],

  /**
   * Include patterns
   * Only these files are included in type checking
   */
  "include": ["src/**/*", "prisma/**/*", "scripts/**/*.ts"]
}
