/**
 * 验证组件模块导出
 *
 * 提供论点验证相关的 UI 组件，用于展示 VerificationAgent 的三重验证结果
 */

// 验证详情弹窗 - 展示事实准确性、逻辑一致性、任务完成度等详细评分
export { VerificationDetailModal } from './VerificationDetailModal';

// 类型定义（如果组件有导出类型）
export type { VerificationDetailModalProps } from './VerificationDetailModal';
